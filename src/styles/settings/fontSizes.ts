export interface IFontSizes {
  caption: string;
  body: string;
  subHeader: string;
  Header: string;
}

export const fontSizes: IFontSizes = {
  caption: "12px",
  body: "16px",
  subHeader: "24px",
  Header: "38px",
};
